A simple program to list all db or get key

Usage:
    list-leveldb [path to leveldb db] [options]
    options:
    - list
      show all database
    - key
      show value of key (convert string to byte)
